<Window x:Class="VectorEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VectorEditor"
        Title="VectorEditor" Height="600" Width="900">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <Border Grid.Column="0" Background="#F5F6F7" Margin="8" CornerRadius="4">
            <StackPanel Margin="10">

                <TextBlock Text="Инструменты" FontWeight="Bold" Margin="0,0,0,10"/>

                <Button Content="Добавить прямоугольник"
                        Command="{Binding StartDrawingRectangleCommand}" Margin="0,0,0,6"/>

                <Button Content="Переместить фигуру"
                        Command="{Binding StartMovingCommand}" Margin="0,0,0,6"/>

                <Button Content="Очистить холст"
                        Command="{Binding ClearCommand}" Margin="0,0,0,10"/>

                <!-- [НОВОЕ] Undo/Redo -->
                <TextBlock Text="История действий" FontWeight="Bold" Margin="0,10,0,10"/>
                <Button Content="Отменить" Command="{Binding UndoCommand}" Margin="0,0,0,6"/>
                <Button Content="Вернуть" Command="{Binding RedoCommand}" Margin="0,0,0,6"/>

            </StackPanel>
        </Border>

        <!-- Холст -->
        <Border Grid.Column="1" Background="White" Margin="10" BorderBrush="#E0E0E0" BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Canvas x:Name="DrawingCanvas"
                        Background="Transparent"
                        Width="1200"
                        Height="900"
                        MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                        MouseMove="Canvas_MouseMove"
                        MouseLeftButtonUp="Canvas_MouseLeftButtonUp">
                    <ItemsControl ItemsSource="{Binding Shapes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Rectangle Width="{Binding Width}"
                                           Height="{Binding Height}"
                                           Fill="{Binding Fill}"
                                           Stroke="{Binding Stroke}"
                                           StrokeThickness="2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
